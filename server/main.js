import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
    // AccountsTemplates.addField({
    //     _id: 'phone',
    //     type: 'tel',
    //     displayName: "N° de téléphone",
    // });
});
